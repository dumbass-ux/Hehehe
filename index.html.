<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Will You Be My Valentine?</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #fce4ec;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            text-align: center;
            position: relative;
        }
        h1 {
            color: #ff69b4;
            font-size: 3em;
            margin-bottom: 20px;
        }
        .button {
            background-color: #ff69b4;
            color: white;
            font-size: 1.2em;
            padding: 15px 30px;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            margin: 10px;
        }
        .button:hover {
            background-color: #ff1493;
        }
        img {
            width: 250px;
            border-radius: 20px;
            margin-bottom: 30px;
        }
        .heart {
            position: absolute;
            top: -50px;
            left: 50%;
            animation: fall 3s infinite;
        }
        @keyframes fall {
            0% {
                transform: translateY(0) scale(1);
            }
            100% {
                transform: translateY(100vh) scale(1.5);
            }
        }
    </style>
</head>
<body>
    <div>
        <h1>Will you be my Valentine?</h1>
        <img src="your-photo-link.jpg" alt="Us">
        <button class="button" onclick="alert('Yay! ❤️')">Yes</button>
        <button class="button" onclick="alert('Oh no! 💔')">No</button>
        <div class="heart" style="animation-delay: 0s;">❤️</div>
        <div class="heart" style="animation-delay: 1s;">💖</div>
        <div class="heart" style="animation-delay: 2s;">💘</div>
    </div>
</body>
</html>